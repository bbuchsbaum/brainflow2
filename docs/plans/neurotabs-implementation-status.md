# NeuroTabs population exploration: implementation evidence

Objective: implement [the complete population exploration plan](neurotabs-population-exploration.md), including its linked plotting extension. This ledger records evidence and outstanding work; it does not replace or narrow that plan.

## Current delivery

The first backend increment supplies reusable, equally weighted descriptive reductions in [`field_table::population`](../../core/field_table/src/population.rs). `FieldMoments` accepts finite measurements with optional validity masks and exposes local eligible/valid counts, mean, sample SD, mean absolute magnitude, cancellation and sign counts. Empty selections produce unavailable values. Spatial blocks and disjoint observation-block merges share the kernel. Numerical arrays remain outside frontend state.

Existing [file materialization](../../core/field_table/src/materialize.rs) now uses centered f64 moments and local denominators. Mean-only streaming uses compact mean/count storage; role reductions use blocks of 16,384 locations for numerical scratch. The existing role-loader still retains decoded input arrays, and z-score export still holds full-grid moments. These are not certified memory-bounded live-evaluation paths.

The SD reducer now exports sample SD with `ddof=1`. Existing descriptive z-scores retain `ddof=0`, use cohort moments computed before rounding the exported mean, and become unavailable at zero dispersion. This is not a group t-test. Output manifests state the observation unit and numerical/missingness conventions; manifest version 2 invalidates earlier cached calculations. Volume reductions check affine compatibility in addition to dimensions. Matching grids do not prove anatomical alignment.

Explicit participant summaries are now available from the Population lens's **Analysis unit** control. Users declare an exact participant-ID metadata column or explicitly declare each observation a distinct person; names and filenames do not infer identity. The default remains an observation summary. Participant mode either requires one selected row per person or explicitly averages that person's selected rows before giving each person equal weight. Complete, uniquely keyed metadata is required. Supported table, discovery and CSV/TSV manifest imports retain a record per observation independently of the compact preview; legacy datasets with only an incomplete preview remain ineligible for metadata-dependent grouping.

Native fields reduce within each participant at each voxel with f64 intermediate means, then compute between-participant moments and local valid counts. Probe summaries first reduce the spatial response within each observation, then reduce those responses within participants; with differing spatial missingness this is not necessarily the value obtained by spatially averaging the participant mean map. Original observations remain in the plot, gallery and focus view. Participant configuration changes reuse the sampled context and preserve membership, reference and probe. The configuration is session state, not yet a saved analysis recipe or a model adapter.

## Requirements ledger

The frontend increment extends the existing Studio store with explicit working membership, fixed/complement reference semantics, pinned/hover probes, relationship-fit identity and bounded working-selection undo/redo. Eligibility is derived separately from list search, issue highlighting and sorting. Metadata filters now refuse incomplete or ambiguous keyed rows, and the strip reports the context issue. Search preserves the focused person even when hidden from the list; valid eligibility changes reconcile focus, including clearing it for an empty context. Bootstrap resets these definitions atomically and advances an import generation. The population-probe panel now wires working-selection controls and frontend plot-query identity. Reference controls and durable live-summary provenance remain unfinished.

Population sampling now uses a CPU cache with a 512 MiB default decoded-payload budget and LRU eviction. Cache misses create a private single-file NIfTI snapshot, compute its SHA-256 and decode those same bytes on one admitted blocking worker. Warm hits revalidate filesystem stamps, including inode/change time on Unix. This freshness shortcut does not detect changes invisible to those stamps. Source files are capped at 2 GiB for snapshotting; header dimensions enforce conservative decode admission. Decoder scratch/process overhead are not certified by the resident payload budget. Panel stop/query replacement now requests cooperative native cancellation; dataset-scoped cache teardown remains unfinished.

Multi-frame sources require explicit frame selection instead of silently using frame zero; one frame is sampled without allocating a full temporal series at every voxel. Frozen-source digests can be supplied for mismatch rejection. The sample-frame metadata retains per-observation source revision, frame index, finite count and failure reason. This is not yet whole-query input freezing, a temporal-axis contract or complete participant/support/units provenance. Spatial means, medians, interpolated quantiles and dispersion moments avoid float32 intermediate overflow.

The Studio center now includes [`PopulationProbePanel`](../../ui2/src/components/studio/PopulationProbePanel.tsx): an all-observation point plot, pinned crosshair or hover probe, radius control, selected descriptive mean, missing count and per-source status. Clicking focuses an original observation; Shift-click or the observation table changes working membership. All/none/focused-only and undo reuse the sampled frame. Hiding the plot preserves its probe and selection. The shared plot encoder retains row identity through missing-value filtering and supplies keyboard focus/selection equivalents.

[`PopulationProbeController`](../../ui2/src/services/studio/PopulationProbeController.ts) owns one issued sampling call plus the latest pending definition, coalesces requests, and rejects obsolete completions. Its query records workspace/import/set/feature identity, resolved context sources and spatial probe. Focus, reference and working membership do not cause resampling. Previous results remain labeled during a new query within the same dataset; changing dataset identity clears them. Ambiguous/unavailable bindings and unresolved frame selectors are refused. [`PopulationProbeActions`](../../ui2/src/services/studio/PopulationProbeActions.ts) owns store mutations and hover-subscription disposal. This lifecycle now cancels issued scalar/trace calls through SampleProvider and the native sampling ticket registry. Immutable saved-query source retention and native cross-workspace hover provenance remain unfinished.

Native sampling now accepts optional expiring UUID tickets and exposes `cancel_population_sample`. The bounded registry retains cancellation-before-start tombstones and rejects ticket replay. Cancellation interrupts cache admission waits and is checked between members, during snapshot copy, and before/after decode and sample closures. Issued decoder calls and template-service source resolution are cooperative boundaries, not forcibly preemptible jobs; their permits remain owned until they exit. The frontend rejects an aborted promise immediately and the backend refuses to publish its partial result. Clients without a ticket retain the existing call shape.

A whole-query source guard captures all resolved file identities before member sampling and rechecks before returning. Changes, deletions and missing-to-available transitions invalidate the complete result. This is optimistic filesystem consistency using the existing stamps, not immutable retention of every source or protection against changes invisible to metadata. Per-source digests continue to identify decoded bytes.

The live-slice increment adds [`PopulationSliceEngine`](../../core/api_bridge/src/population_slice.rs) and the `evaluate_population_slice` / `release_population_slice` commands. It gathers unique native voxels needed by one visible plane, retains a bounded observation matrix, and reuses that matrix for working-selection, summary and focus changes. It computes mean, sample SD, cancellation and coverage with the shared kernel; the focused image contains actual observed samples. A missing file, incompatible grid, unresolved 4D frame or frozen-digest mismatch fails the whole field query. Source guards run before and after evaluation. Rotated orthogonal grids are accepted; sheared grids currently require explicit resampling. Display sampling is nearest-neighbor and float32; this is not original-f64 export equivalence.

[`PopulationLens`](../../ui2/src/components/studio/PopulationLens.tsx) shows the live summary and focused original using the shared viewport and image-surface primitives. Audited volume imports now open this lens. The Studio coordinator bypasses ordinary file/GPU loading and file materialization for population focus changes. Both panes share geometry, crosshair and a fixed value scale for mean/individual comparison. Nonnegative summaries have a separate adjustable scale. Clicking pins a probe; hover feeds the existing plot preview; navigation preserves the pinned location. Holding the preview button temporarily removes the focused observation from the field query and releasing restores it without changing canonical membership. The participant controls now also support removal of every selected row belonging to the focused person, while the focused image remains an original observation.

[`PopulationSliceService`](../../ui2/src/services/studio/PopulationSliceService.ts) owns one active native query plus its latest replacement, cancellation tickets, decoded response arrays and reference-counted bitmap leases. Dataset changes clear displayed results and request native plane-cache release; mounted view leases protect retired images until React releases them. The native plane cache has a 128 MiB sample/geometry payload budget and one admission slot. Decoded source-cache residency, result arrays, numerical scratch, JSON IPC and bitmap memory remain separate allocations; dataset-scoped decoded-source teardown is still outstanding. Component close releases its plane cache, and previous results retain their own query/geometry labels while pending.

| Plan area | Current evidence | Remaining before acceptance |
|---|---|---|
| M0: state separation | Existing store extended with context resolution, explicit working membership, fixed/complement references, pinned/hover probes, relationship identity and selection undo/redo; search/focus/filter regressions covered | Complete cross-view transitions, reference controls and live summary identity; persisted participant definitions and history |
| M0: scientific input contracts | Volume export checks matching affines; explicit participant IDs from complete keyed metadata or a distinct-person declaration, with single-row or within-person mean semantics; complete per-observation import metadata | Persisted identity/provenance, feature roles/units, immutable source revisions, declared support/mask adapters, strict duplicate/ambiguous input handling |
| M0: sampling/plot contracts | Source snapshot digests, explicit 4D frame selection, finite counts and failure reasons reach typed sample-frame metadata | Whole-query revisions, full participant/probe/support identity, units, physical axis semantics and explicit live/saved bindings |
| M0: baseline and fixtures | Analytic 80-observation numerical populations and independent batch oracle added | Importable audited 80-map fixture, spatial displacement/noise/repeated-measure cases, native app latency and memory baseline |
| M1: reductions | Finite/masked kernel, legacy export integration and live native-plane evaluation with source validation | Incremental selection updates/removals and rebuild policy; complete support adapters; view/export equivalence |
| M1: population lens | Simultaneous live summary/focused views, common geometry and value scales, linked plot, hover/pin, observation/participant preview-without, source identities and controlled teardown; native evaluator and synthetic real-canvas harness verified separately | Native app integration walkthrough and end-to-end budgets; subset sensitivity and view/export equivalence, range/lasso brushing and imported/per-observation mask adapters; cutout/ordering native walkthrough |
| M2: fluid sets | Existing folder switching and resident-image-set primitives available; working-membership set algebra and bounded undo/redo added | Stable fitted order, cached scrubbing/movie, montage transitions, explicit similarity/factor groups, selection controls and reversible presentation history |
| M3: typed comparisons | Descriptive legacy z-score clearly separated from sample SD | One-sample/Welch/paired estimators, effect/SE/t/df/count outputs, valid pairing and input-role checks, exploratory provenance and frozen recipes |
| M4: relationship explanations | No new implementation | Context-fixed MDS/PCA, fit quality, regional refits, spatial distance attribution and explicit observed/reconstructed views |
| M5: breadth | Numerical kernel is independent of geometry | Audited parcel/surface adapters and identity, robust summaries, pivot views, validated model adapters and larger-set evidence |
| P: linked plot authoring | First hover/pin population plot uses SampleProvider and the shared grammar encoder; selection summaries reuse the frame; collision-safe observation metadata joins preserve row grain | Native hover acceptance, multiple sets/probes, trajectories and real temporal axes, grouping/faceting/layers, defined bands, cached regrouping, saved recipes/export |
| Performance/resource contract | Kernel supports spatial blocks; accumulator reports owned heap bytes; sampled sources have bounded resident payloads and cancellation-safe decode admission | End-to-end p50/p95/p99, exact visible-support evaluation, complete reduction/GPU budgets, dataset teardown and finer-grained cancellation/latency evidence, full-pipeline stale-publication checks, cold/remote/pressure tests |
| Scientific and interaction acceptance | Numeric shared/opposing/minority examples, masks, large offsets, scaling and partition checks | Complete plan fixture matrix, independent inference evidence, native walkthrough and interpretation evaluation |

## Verification receipts

- Before implementation, three reducer regressions failed: valid values were lost to global missingness, SD used the wrong denominator, and finite float32 inputs overflowed accumulation. Local receipt: `/private/tmp/neurotabs-population-red.log`.
- [`population_moments.rs`](../../core/field_table/tests/population_moments.rs) compares streaming/block results against closed-form examples and an independent two-pass centered calculation. It checks missing versus zero, sample counts, inclusive near-zero endpoints, translation at large offsets, signed scaling, order changes, atomic rejection and bounded spatial scratch.
- Materialization regressions in [`lib.rs`](../../core/field_table/src/lib.rs) exercise actual NIfTI outputs, stale calculation-cache invalidation, undefined standardized values, and rejection of equal-size grids with different world affines.
- Final focused suite: `cargo test -p field_table` passed 55 tests (47 unit, 8 integration), with no ignored tests. Receipt: `/private/tmp/neurotabs-population-tests.log`.
- Native workspace unit/integration run: 786 passed, 27 ignored, including GPU lifecycle tests, render goldens and shader contracts. The sandboxed attempt could not obtain an adapter. The native run's final rustdoc phase encountered crate-metadata resolution errors after overlapping Cargo builds; a subsequent serial `cargo test --workspace --doc` completed successfully (18 doc-test suites, zero examples). These are separate receipts, not one clean invocation: `/private/tmp/neurotabs-workspace-tests-native.log` and `/private/tmp/neurotabs-workspace-doctests.log`. The final focused suite additionally covers the affine-validation test and lint cleanup added during this run.
- `cargo clippy --workspace --all-targets` completed with warnings. After fixing both warnings introduced in this increment, `cargo clippy -p field_table --all-targets --no-deps -- -D warnings` passed. Receipts: `/private/tmp/neurotabs-clippy.log` and `/private/tmp/neurotabs-population-clippy.log`.
- `cargo fmt --all -- --check` passed; scoped diff whitespace and local documentation links also passed. Kernel and export results do not establish UI or end-to-end performance acceptance.
- Population-state increment: 93 tests passed across 10 Studio import/store/service/component suites, including 23 focused state/coordination/strip tests. Receipt: `/private/tmp/neurotabs-state-regression.log`. Ran the installed Vitest binary from `ui2` with `NODE_OPTIONS=--no-experimental-webstorage` (the shell lacked `pnpm`, and the Node experimental storage global interfered with jsdom in the initial invocation).
- `ui2/node_modules/.bin/tsc -p ui2/tsconfig.app.json --pretty false` and ESLint over the 11 changed TypeScript source/test paths passed. Receipts: `/private/tmp/neurotabs-state-typecheck.log` and `/private/tmp/neurotabs-state-lint.log`. No native visual or linked-plot acceptance is claimed for this state increment.
- Source/sampling increment: native `cargo test -p api-bridge` passed 195 tests, with 2 ignored (152 unit tests and 43 integration tests; empty suites and doc tests also completed). This includes real-NIfTI snapshot/digest/eviction tests, canceled-caller admission ownership, explicit 4D extraction, stale-source rejection, spatial numerical overflow regressions and existing GPU gates. Receipt: `/private/tmp/neurotabs-api-native.log`. The prior sandboxed unit run failed two adapter-dependent tests; native execution resolved both.
- The sample-provider suite passed 16 tests, including frame/digest forwarding and source-error retention for scalar and trace paths. TypeScript and scoped ESLint passed. Receipts: `/private/tmp/neurotabs-source-plot-tests.log`, `/private/tmp/neurotabs-source-typecheck.log`, `/private/tmp/neurotabs-source-lint.log`.
- `cargo clippy -p api-bridge --all-targets --no-deps` completed with existing warnings at the surface-remove call and parcel-overlay test arithmetic; no warning points to the changed sampling/cache code. Receipt: `/private/tmp/neurotabs-sampling-clippy.log`. The command/permission inventory remains synchronized (110 commands); no new command was registered.

- Population-probe UI increment: 173 tests passed across 22 Studio, plot, service and population-store suites. Coverage includes latest-query coalescing, stale completions, StrictMode restart, invalid sampled IDs, missing-row plot links, focus versus selection, pinned hover and teardown. Receipt: `/private/tmp/neurotabs-live-probe-regressions.log`.
- TypeScript and scoped ESLint passed for the increment. Receipts: `/private/tmp/neurotabs-live-probe-types.log`, `/private/tmp/neurotabs-live-probe-lint.log`.
- The dev-only [`population-harness.html`](../../ui2/population-harness.html) uses the real panel, store and shared encoder with a clearly labeled synthetic sampler: 40 observations at +3 and 40 at −1, selection mean +1. Playwright checked point-to-focus, focused-only/all selection means and no horizontal overflow at 1100 and 480 px. It exposed an axis intercepting point clicks; axes now ignore pointer events and linked points have padded bounds. Screenshots: `/private/tmp/neurotabs-population-desktop.png`, `/private/tmp/neurotabs-population-narrow.png`; receipt: `/private/tmp/neurotabs-population-visual.log`. This is isolated browser evidence, not native Tauri sampling, brain-view integration, full-shell layout or latency acceptance.

- Cancellation/source-consistency increment: native `cargo test --workspace` passed 801 tests with 27 ignored; all doc-test suites also completed. This includes the new cancellation-before-start/replay/deadline/capacity tests, canceled cache admission and result suppression, changing/deleted/newly available source checks, render goldens, shader contracts and GPU resource cycles. Receipt: `/private/tmp/neurotabs-cancellation-workspace.log`.
- The expanded UI run passed 193 tests across 23 suites, including scalar/trace cancellation tickets, already-aborted calls, late native completion, listener cleanup, controller stop/replacement and readable bridge errors. TypeScript and scoped ESLint passed. Receipts: `/private/tmp/neurotabs-cancellation-ui-full.log`, `/private/tmp/neurotabs-cancellation-types.log`, `/private/tmp/neurotabs-cancellation-lint.log`.
- Bindings regenerated with `cargo run -p xtask -- ts-bindings` because the worktree's Cargo alias was unavailable; the API package rebuilt successfully. The strict permission checker passed with 111 commands. Receipt: `/private/tmp/neurotabs-cancellation-bindings.log`. Existing unrelated generated `dist`/desktop-schema changes remain outside the scoped source commit.

- Workspace Clippy completed with warnings. The new source-guard type-complexity warning was fixed; the eight source-cache tests then passed again, and scoped api-bridge Clippy reports only the existing surface-call borrow and parcel-test arithmetic warnings. `cargo fmt --all -- --check` and scoped diff whitespace passed. Receipts: `/private/tmp/neurotabs-cancellation-clippy.log`, `/private/tmp/neurotabs-cancellation-clippy-final.log`, `/private/tmp/neurotabs-cancellation-cache-final.log`. The full native suite preceded this structure-only cleanup; the focused cache rerun covers the final structure.

- Live-slice increment: native `cargo test --workspace` completed successfully with 807 passed, 27 ignored, and all doc-test suites completed. The six new evaluator tests cover the analytic 80-person opposing population, missing versus zero, empty selection, matching world geometry in three orientations, incompatible affines, explicit 4D frames, source refresh/digests, cancellation and context-scoped plane release. Receipt: `/private/tmp/neurotabs-slices-workspace.log`.
- Native evaluation timing fixture: 80 maps × 204,800 float32 measurements (65,536,000 decoded bytes), 128 × 128 requested raster, debug build. Cold evaluation 1516.73 ms; twenty warm focus/selection changes p50 6.35 ms, p95 6.95 ms, maximum 6.95 ms; retained plane sample/geometry payload 3,080,192 bytes. Each changed mean/focused value is checked analytically. Receipt: `/private/tmp/neurotabs-slices-native-receipt.log`. This excludes IPC, canvas presentation and full-process memory; it does not satisfy the end-to-end latency gate.
- UI regression run: 236 tests passed across 27 Studio/import/population/plot suites. New tests cover latest-only native requests, late bitmap conversion, reference-counted image teardown, fixed and explicitly fitted scales, source/raster mismatches, coordinate linking, observation-level held preview, canonical membership preservation, native-load bypass, canceled-comparison restart and the Population import default. Receipt: `/private/tmp/neurotabs-slices-ui-final.log`.
- TypeScript, scoped ESLint, API-package build and 113-command strict permission checks passed. Scoped api-bridge Clippy completed with only existing surface-call borrow and parcel-test arithmetic warnings. Receipts: `/private/tmp/neurotabs-slices-types.log`, `/private/tmp/neurotabs-slices-lint-final.log`, `/private/tmp/neurotabs-slices-api-build.log`, `/private/tmp/neurotabs-slices-bindings.log`, `/private/tmp/neurotabs-slices-clippy.log`.
- The dev population harness now renders the real paired canvas views with clearly labeled synthetic ellipse fields. Playwright verified image-to-pin, shared plot-to-focus without membership changes, held preview and restoration, hover, and 1100/480 px layouts with the lower plot reachable by scrolling. Screenshots: `/private/tmp/neurotabs-slices-desktop.png`, `/private/tmp/neurotabs-slices-narrow.png`, `/private/tmp/neurotabs-slices-narrow-plot.png`; receipt: `/private/tmp/neurotabs-slices-visual.log`. This remains synthetic service/browser evidence, separate from native evaluator correctness and from the outstanding native app walkthrough.

## Individual cutout gallery increment

The Population lens now expands a pinned neighborhood into actual observation cutouts. Width is specified in millimeters; the grid shares orientation and the individual-map scale. One sprite and one canvas display up to 80 context members per page, with stable IDs, valid-pixel counts, keyboard focus and Shift selection. Large-view navigation preserves the pinned cutout location. Hiding/reopening the gallery preserves the probe and canonical population state. Source order is explicit; quantile witnesses and regional ordering remain to be implemented.

The native evaluator gathers the union of main-plane and cutout voxels in its existing bounded matrix. Gallery member IDs affect the output, not cache identity, so changing pages reuses samples. Requests accept at most 96 cutouts and a 64-pixel square; the UI uses 40-pixel cutouts. Cutouts can occupy a different through-plane location from the main view. They remain original nearest-neighbor observed values, with missing values transparent. There are no new GPU volume registrations or bridge commands.

Evidence:

- Native `cargo test -p api-bridge` completed with 209 passed and 2 ignored, including resource/integration tests; the final focused evaluator rerun verifies coordinate-ramp cutouts in all three orientations, pinned geometry, identity/page reuse, shifted peaks, missing coverage, source freshness, cancellation and release. Receipts: `/private/tmp/neurotabs-cutouts-api.log`, `/private/tmp/neurotabs-cutouts-native-final.log`.
- 99 tests passed across 14 Studio, population and linked-plot suites. Tests cover one-canvas composition, mouse/keyboard focus versus selection, pinned navigation, sprite packing, reordered-response rejection, bitmap failure and lease cleanup. TypeScript and scoped ESLint passed. API bridge Clippy completed with only the existing surface-call borrow and parcel-test arithmetic warnings (`/private/tmp/neurotabs-cutouts-clippy.log`). Receipts: `/private/tmp/neurotabs-cutouts-ui-full.log`, `/private/tmp/neurotabs-cutouts-types.log`, `/private/tmp/neurotabs-cutouts-lint.log`.
- The synthetic browser harness renders 80 actual cutouts. Playwright verifies opposing signed pixel colors, linked focus, Shift selection, observed hover values, hide/reopen identity and desktop/narrow scrolling. Screenshots: `/private/tmp/neurotabs-cutouts-desktop.png`, `/private/tmp/neurotabs-cutouts-narrow.png`; receipt: `/private/tmp/neurotabs-cutouts-visual.log`. This is isolated canvas/browser evidence, not native IPC or full-shell acceptance.
- The realistic native fixture retains 80 × 204,800 source measurements and returns an 80 × 40 × 40 cutout gallery alongside the 128 × 128 main plane. Every returned cutout value and each changing summary are checked analytically. The final debug-build receipt reports cold evaluation 1502.82 ms; twenty warm updates p50 7.58 ms, p95 8.25 ms, maximum 8.29 ms; retained sample/geometry payload 3,105,792 bytes. IPC, browser presentation, full-process memory and sustained-session behavior remain separate measurements.

## Response ordering and observed witnesses increment

The Studio center now shares its existing probe controller between the population plot and image lens. The gallery offers explicit ordering by the pinned point or sphere response and an actual-observation witness sample, up to twelve equally spaced empirical ranks including the endpoints. Equal values retain source order. Finite zero remains valid; unavailable responses are placed last in the full order and omitted from the witness sample with a visible explanation. Expanding returns every observation in the fitted order; resetting restores source order. The plot always retains every observation, including unavailable rows, and follows the same response order.

The arrangement retains its query, probe, observation IDs and source revisions. Hover, focus, selection, and a new pinned location do not refit it. Old-probe and changed/unverified-source notices distinguish the fixed fit from current values and images. Dataset or source-binding changes invalidate the arrangement. Numerical frames stay in the existing controller outside Zustand; ordering and witness expansion reuse them without native resampling. The values panel owns controller lifecycle, while the image lens is a subscriber. This is an in-memory presentation fit; durable recipe history and the later similarity-order workflow remain unfinished.

Evidence:

- 107 tests passed across 15 Studio, service, population and linked-plot suites. New tests independently enumerate the 80-observation empirical-rank witness sample, test ties/missingness/identity validation/source revisions, verify fixed fits across pending and completed probe changes, and exercise the real lens-plus-plot workflow. The integration test verifies focus preserves membership and order/witness expansion/reset issue no additional probe-sampling calls. Receipt: `/private/tmp/neurotabs-witness-ui.log`.
- TypeScript and scoped ESLint passed: `/private/tmp/neurotabs-witness-types.log`, `/private/tmp/neurotabs-witness-lint.log`. No native computation or command surface changed in this increment; the cutout evaluator evidence above remains its backend basis.
- The synthetic browser harness verifies 80 response-ordered cutouts, twelve distinct actual witnesses with both response signs, all eighty plot points, focus without membership changes, stable order after repinning, expansion/source-order reset and narrow scrolling. Screenshots: `/private/tmp/neurotabs-witness-desktop.png`, `/private/tmp/neurotabs-witness-narrow.png`; receipt: `/private/tmp/neurotabs-witness-visual.log`. This does not establish native IPC, full-shell behavior, or end-to-end latency.

## Magnitude and observed sign distribution increment

The live population image now offers mean absolute magnitude through the existing stable moment kernel and bounded evaluator. It uses the nonnegative display scale; the focused observed image retains its fixed signed scale. Missing measurements have local denominators, finite zero remains valid, and empty or wholly unavailable selections remain unavailable.

The linked probe panel displays negative, near-zero and positive counts and shares for the working selection. The near-zero interval has inclusive endpoints, is expressed in response-value units and resets on dataset/feature changes. Its denominator is the number of finite selected observation responses; missing selected responses are reported separately. Changing this interval or membership reuses the sampled frame. The panel also shows mean absolute response and cancellation across those responses. For a sphere, it explicitly distinguishes summarizing per-observation regional responses from the voxelwise reductions in the population images. These descriptive outputs do not estimate true-effect prevalence or inferential uncertainty.

Evidence:

- Three new description tests failed before implementation (`/private/tmp/neurotabs-signs-red.log`). The final UI/service run passed 111 tests in 15 suites, including shared/opposing/minority populations with the same mean, inclusive interval boundaries, missing versus zero, selected denominators, empty selections, large opposite finite doubles and no resampling during sign-interval/selection changes. Receipt: `/private/tmp/neurotabs-signs-ui.log`.
- Native API bridge suite: 209 passed, 2 ignored (`/private/tmp/neurotabs-signs-api.log`). After adding the final magnitude missingness/wire-name regression, all nine evaluator tests passed (`/private/tmp/neurotabs-signs-native-final.log`). The native opposing fixture verifies magnitude 2 versus cancellation 1; the added fixture verifies local finite counts, valid zero, missing-only and empty selections, and `meanAbsolute` wire spelling.
- TypeScript and scoped ESLint passed; API bridge Clippy completed with the existing surface-call borrow and parcel-test arithmetic warnings. Receipts: `/private/tmp/neurotabs-signs-types.log`, `/private/tmp/neurotabs-signs-lint.log`, `/private/tmp/neurotabs-signs-clippy.log`.
- The synthetic browser harness verifies sign shares, the inclusive −1 boundary, magnitude/cancellation readouts, empty/restored selection, the mean-absolute image option with unchanged observed scale, and desktop/narrow layout. Screenshots: `/private/tmp/neurotabs-signs-desktop.png`, `/private/tmp/neurotabs-signs-narrow.png`; receipt: `/private/tmp/neurotabs-signs-visual.log`.

Native desktop walkthrough remains open: computer-use surface discovery was attempted twice on 2026-09-06 and both attempts failed with `Sky Computer Use service startup request failed`. No app interactions or desktop acceptance are claimed. This prevents that check in the current environment, but does not block the remaining implementation work.

## Participant identity and weighting increment

The new analysis-unit control explicitly chooses participant identity and observation, single-row-per-person, or within-person-mean reduction. The native request validates a complete partition of selected observation IDs, and its result distinguishes selected rows, selected units and locally valid units. Whole-participant exclusion requires a declared identity and removes all selected rows of the focused person without changing the canonical selection or focused original. Plot summaries reuse the cached observation frame, retaining the original plotted points and adding participant identity. An incomplete old frame cannot become a complete new participant summary while context expansion is pending.

Evidence:

- `cargo test --workspace` passed 813 tests, with 27 ignored, and completed all doc-test suites. Receipt: `/private/tmp/neurotabs-participants-workspace.log`. New numerical tests independently distinguish row mean 2 from participant mean 4 for unequal row counts, retain local missingness/counts, and verify that f64 intermediate participant means preserve small between-person spread. Native tests check partition rejection, analysis-unit coverage, original focus, cached-source reuse, whole-person omission and empty groups.
- UI regression run: 120 tests passed across 16 suites (`/private/tmp/neurotabs-participants-ui-final.log`). Subsequent final guard checks passed 20 tests (`/private/tmp/neurotabs-participants-final-guards.log`); the expanded six-test probe panel suite includes the new stale-context recovery case (`/private/tmp/neurotabs-participants-stale-ui.log`). Coverage includes exact metadata joins, invalid identity refusal, strict single-row/subset recovery, probe reuse, whole-person held exclusion and unchanged focus/selection, invalid coverage units and no fallback from unknown participant identity.
- TypeScript, scoped ESLint, Rust formatting and the 113-command strict permission inventory passed. Receipts: `/private/tmp/neurotabs-participants-types-final.log`, `/private/tmp/neurotabs-participants-lint.log`, `/private/tmp/neurotabs-participants-fmt.log`. No command was added; participant aggregation extends the existing slice request/result contract.
- Workspace Clippy completed with existing warnings; none points to the participant changes. Receipt: `/private/tmp/neurotabs-participants-clippy.log`.
- Playwright verified the real controls and paired canvases against a labeled synthetic 80-row/41-participant fixture. Row mean +1 becomes participant mean −0.9024, sign counts use 41 participants, held exclusion removes the forty rows of the focused participant and release restores the map while plot selection remains unchanged. Strict single-row rejection recovers on a valid subset. Desktop and 480px layouts were inspected; the browser and Vite server were closed and the final ownership audit was clear. Receipts: `/private/tmp/neurotabs-participants-visual.log`, `/private/tmp/neurotabs-participants-desktop.png`, `/private/tmp/neurotabs-participants-narrow.png`. This is synthetic UI evidence, not native IPC or a full-shell acceptance claim.

The native desktop walkthrough, end-to-end latency, saved participant definitions, model semantics and participant-aware full export remain open. Complete metadata for supported table/discovery imports is delivered in the following increment. These gates are not replaced by the passing component and kernel checks.

## Complete observation metadata increment

`StudioMemberSummary.designValues` carries imported string metadata for each original observation. Table imports retain the observation ID and nonexcluded design columns; discovery retains all captured design values. CSV/TSV manifest imports retain declared observation metadata beyond the preview limits, excluding feature payload and locator/selector columns. Duplicate, blank or padded headers and ragged table rows are rejected. Missing declared manifest metadata is an error. This adds an optional, legacy-compatible field to the existing bridge type; no command is added.

`studioMetadata` provides one validated lookup for participant identity, eligibility filters, search, sorting, table display and probe labels. Full observation records take precedence over the preview, and an incomplete authoritative record cannot be repaired silently from it. Legacy preview metadata is usable only when it covers the requested observations. The design pane renders up to eighty observations per page in the chosen order. Paging and search preserve focus; explicit metadata filters alter eligibility. Quick filters retain the existing compact four-column/six-value presentation.

Scalar and trace sample frames now join metadata by observation ID without changing row count, order, sampled values, coverage or uncertainty fields. Reserved or colliding names receive explicit aliases recorded in `meta.designColumnAliases`. This is a foundation for grouped plot authoring: metadata remains nominal strings here, not inferred numerical covariates, physical time, participant-level uncertainty or a completed multi-set plotting grammar. Saved metadata revisions, recipes and export parity remain separate work.

Evidence:

- `cargo test --workspace` passed 816 tests, with 27 ignored, and completed all doc-test suites (`/private/tmp/neurotabs-metadata-workspace.log`). Native regressions cover 160 table observations with a five-row preview, manifest metadata beyond the first four columns and six rows, exclusion of imaging payload/locators, malformed headers and late ragged rows, required declared columns, discovery identity, full versus legacy serialization and image-set labels beyond the preview.
- The broader UI run passed 186 tests across 22 suites (`/private/tmp/neurotabs-metadata-ui-final.log`). The final import/paging/derived-state guard suite passed 28 tests in three suites, recorded in `/private/tmp/neurotabs-metadata-final-guards.log`. Tests check late-row participant identity and filters, authoritative metadata refusal, collision-safe plot joins including repeated sample rows, scalar sampling and bounded table paging without changing focus.
- Rust formatting, the 113-command strict permission inventory and workspace Clippy passed; Clippy retains existing surface-call borrow and parcel-test arithmetic warnings (`/private/tmp/neurotabs-metadata-clippy.log`). TypeScript and scoped ESLint receipts: `/private/tmp/neurotabs-metadata-types-final.log`, `/private/tmp/neurotabs-metadata-lint.log`, `/private/tmp/neurotabs-metadata-lint-final-guards.log`. The shared binding was regenerated and the API package built.
- The development-only population harness supports `?metadata`: 161 observations and 122 participants with a six-row preview. Playwright verified all three pages, focus independence, late-site search/filter over 81 observations, participant counts before/after filtering, and desktop/narrow layouts. Receipts: `/private/tmp/neurotabs-metadata-visual.log`, `/private/tmp/neurotabs-metadata-desktop.png`, `/private/tmp/neurotabs-metadata-narrow.png`. Browser and server were closed; the final ownership audit was clear. This is synthetic UI evidence, not native IPC acceptance.

## Common binary mask increment

The Population lens now provides Choose/Change/Clear mask controls. One explicit binary NIfTI mask applies consistently to the live summary, focused original, individual cutouts and scalar/trace probes. Excluded locations are unavailable, including transparent coverage pixels; included measured zeros remain valid. Changing support preserves membership, focus, the spatial probe and compatible color scales. Pending results and file choices are guarded against dataset/import changes.

Native preparation checks all mask voxels for finite exact 0/1 values, a single spatial frame, and matching dimensions and world affine. It refuses implicit thresholding or resampling. A query-owned bit mask holds one process-wide admission permit, bounding prepared bits to 32 MiB separately from decoded source-cache accounting. Mask sources participate in snapshot consistency checks and result provenance. Frozen digest mismatches and masked member source/grid failures reject the query; saved response arrangements detect changed mask digests even when the observation files are unchanged.

Evidence:

- `cargo test --workspace` passed 819 tests with 27 ignored; doc-test suites completed (`/private/tmp/neurotabs-mask-workspace.log`). Three new native tests cover binary/grid/frame rejection, full-mask validation outside the visible slice, real zero versus excluded values, cutout/probe agreement, counts, all-zero masks, cache invalidation, frozen hashes and cancellation-aware admission retained through the last owner.
- The UI regression run passed 153 tests across 19 suites (`/private/tmp/neurotabs-mask-ui-complete.log`); the final provenance guard rerun passed 37 tests (`/private/tmp/neurotabs-mask-final-guards.log`), including shared query support, returned digest validation, arrangement freshness, stale chooser results and scale preservation. TypeScript, scoped ESLint, Rust formatting and the 113-command permission inventory were checked. Workspace Clippy completed with pre-existing warnings; the new command-arity warning received a narrowly scoped annotation to retain the existing command operands.
- Playwright exercised the real mask controls against the explicitly synthetic `?mask` harness: applying/clearing support changes canvases and probe availability without changing focus. Desktop and narrow layouts were inspected. Receipts: `/private/tmp/neurotabs-mask-visual.log`, `/private/tmp/neurotabs-mask-desktop.png`, `/private/tmp/neurotabs-mask-narrow.png`. Browser and Vite server were closed and the final ownership audit found no automated top-level browser processes.

The computer-use service failed to start, so the real Tauri import-to-mask walkthrough remains open. This increment does not deliver per-observation/imported mask declarations, surface support adapters, saved mask retention or participant-aware masked full export. End-to-end masked interaction latency also remains to be measured.

## Next implementation path

Verify the native app path from audited import through live mean/focus, pinned values and restored sensitivity preview, including end-to-end latency and full-shell layout. Extend the live evaluator to participant provenance and masked view/export parity; extend witness rules to largest deviations and maps nearest a stated summary, with explicit spatial distance definitions. Add immutable saved-source retention, decoded-source dataset teardown and physical temporal axes. Preserve the complete M0–M5 and P scope while delivering and verifying these increments.
