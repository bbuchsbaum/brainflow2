// Re-export for the canonical filename split used by other surfaces.
// The component itself lives next to RecentLocationsList so that
// pin / unpin / clear-recents share their action helpers.
export { PinnedLocationsList } from './RecentLocationsList';
