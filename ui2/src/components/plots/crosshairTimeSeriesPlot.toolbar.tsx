/**
 * DEPRECATED / EMPTY — superseded in step 2.
 *
 * The step-1 sphere-radius toolbar control was folded into the generic
 * `PlotEncodingPanel` (rendered in-body via `EncodedPlotView`). This file has
 * no remaining exports and can be deleted; it is left as an empty module only
 * because the environment blocked file removal during the edit.
 */

export {};
