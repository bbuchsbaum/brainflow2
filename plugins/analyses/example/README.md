# Example Analysis Plugin

This folder contains a **non‑functional** example manifest to illustrate the
sidecar analysis format.

To make it runnable, add an executable at `bin/roi_stats.py` (or update
`analysis.json.runner.command`) that implements the CLI‑JSON protocol described in
`docs/analysis_plugins.md`.

